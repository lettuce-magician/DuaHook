local Settings = {
    LOGGER = { -- Logger Settings
        ACTIVATED = true, -- Toggles if logger is activated
        WRITE_FILE = true, -- Toggles if logger can write the login a file
        FILE_PATH = "./" -- The log file path
    }
}

return Settings